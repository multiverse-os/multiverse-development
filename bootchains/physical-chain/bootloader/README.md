# Bootloader
Bootloader (common solution includes grub2) provides the GUI for seelcting kernel, typically with the option to select the previously installed kernel for recovery, or a recovery mode for each kernel for fixing issues. 
