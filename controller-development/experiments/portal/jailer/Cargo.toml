[package]
name = "jailer"
version = "0.1.0"
authors = [""]

[dependencies]
clap = { version = ">=2.27.1", default-features = false}
libc = ">=0.2.39"
regex = ">=1.0.0"

portal_util = { path = "../portal_util" }
sys_util = { path = "../sys_util" }

[dev-dependencies]
tempfile = ">=3.0.2"
